# Citekit Web（产品原型）

Vite + React + TypeScript。数据为内存 mock，刷新即重置。

```bash
cd web
pnpm install
pnpm dev
```

浏览器打开 http://localhost:5173

可走通：知识库语料源（上传/网页/飞书/语雀）→ 库片 → 检索工具试检索（含 filter_first / 重排档）→ MCP 工具白名单。
